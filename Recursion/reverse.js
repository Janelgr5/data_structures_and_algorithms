// Write a function called reverse which accepts a string and returns a  new string in reverse.

// let reverse = str => {
//   let reversedStr = "";

//   let helper = helperStr => {
//     if (!helperStr.length) return;
//     reversedStr.concat(helperStr[helperStr.length - 1]);
//     helper(helperStr.slice(0, helperStr.length - 1));
//   }

//   helper(str);
//   return reversedStr;
// }
